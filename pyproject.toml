[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["tests/api"]
addopts = "-v --tb=short"

[tool.ruff]
line-length = 100
target-version = "py39"
extend-exclude = [".venv", "frontend"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
