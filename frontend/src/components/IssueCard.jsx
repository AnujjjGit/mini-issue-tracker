const STATUSES=["Todo","In Progress","Done"];
function slug(value){return value.toLowerCase().replace(/\s+/g,"-")}
export default function IssueCard({issue,onChangeStatus,onDelete}){return <div className="issue-card" data-testid={`issue-card-${issue.id}`}><div className="issue-card-header"><span className="issue-title" data-testid={`issue-title-${issue.id}`}>{issue.title}</span><span className={`badge priority-${slug(issue.priority)}`}>{issue.priority}</span></div>{issue.description&&<p className="issue-description">{issue.description}</p>}<div className="issue-meta"><span data-testid={`issue-status-${issue.id}`}>Status: {issue.status}</span>{issue.assignee_id!=null&&<span> - Assignee #{issue.assignee_id}</span>}</div><div className="issue-actions">{STATUSES.map(status=><button key={status} data-testid={`status-btn-${slug(status)}-${issue.id}`} disabled={issue.status===status} onClick={()=>onChangeStatus(issue,status)}>{status}</button>)}<button className="danger" data-testid={`delete-issue-${issue.id}`} onClick={()=>onDelete(issue)}>Delete</button></div></div>}
